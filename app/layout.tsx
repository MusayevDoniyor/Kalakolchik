import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MindSnap — AI-Powered Memory & Spaced Repetition Telegram Bot",
  description: "Capture thoughts, voice memos, circular videos, and documents instantly. MindSnap schedules smart spaced repetition reminders right inside Telegram.",
  keywords: ["Telegram Bot", "Spaced Repetition", "AI Reminders", "Voice Memo", "MindSnap", "Gemini AI"],
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
